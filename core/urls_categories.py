# core/urls_categories.py
from django.urls import path, include

# Add viewsets and routers later when category endpoints are defined
# from rest_framework.routers import DefaultRouter
# from .views import CategoriaViewSet # Assuming CategoriaViewSet exists in core/views.py

# router = DefaultRouter()
# router.register(r"", CategoriaViewSet, basename="categoria")

urlpatterns = [
    # path("", include(router.urls)),
]

